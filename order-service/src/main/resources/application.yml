spring:
  datasource:
    driver-class:
      name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/order-service
    username: root
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8081
